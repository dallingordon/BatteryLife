=== notes 9/19 ===

--- IDEA (parked, not implemented): per-chemistry loss weighting for pooled training ---
Pooled training is ~84% Li-ion by cells (train cells: Li-ion 515, CALB 17, Zn-ion 60, Na-ion 20 = 612).
Idea: scale the loss per chemistry so the big chemistry doesn't dominate - weight each sample in chemistry c
by N_c^(-alpha), then rescale so the mean weight over the training set is 1 (keeps loss scale / effective lr
comparable to unweighted runs). Flag shape: --chem_loss_alpha (float, default 0 = off), not a boolean.
  alpha=1   -> chemistries contribute equally to the loss
  alpha=0.5 -> softer, sqrt balancing
Approx weights (cell counts as proxy): alpha=1: Li-ion ~0.3, CALB ~9, Zn ~2.5, Na ~7.7
                                       alpha=0.5: Li-ion ~0.7, CALB ~3.8, Zn ~2.0, Na ~3.5
Why attractive:
  - model selection already averages per-chemistry val MAPE with equal weight, so this aligns the training
    objective with the selection criterion
  - in regression it may help Zn-ion (small labels -> small errors under the Li-ion-dominated pooled scaler)
Risk: at alpha=1 CALB's 17 cells get ~9x weight; each cell gives ~100 near-duplicate truncated samples, so
the model may memorize those 17 cells. Would sweep alpha in {0, 0.5, 1}, CPMLP first.
Implementation sketch (when/if wanted): the training loss is already mean(loss * weights) for both regression
and geo_bins, so fill the per-sample `weights` in Dataset_pooled (train flag only) from chemistry_counts();
no model or training-loop change. Existing --weighted_loss is a different thing (KDE over labels).
Alternative: oversample small chemistries with a weighted sampler (equivalent in expectation, noisier, more plumbing).
Orthogonal to --chem_fusion (can combine).
Status: dgordon pondering.

--- Conditioning granularity: finer options (noted, not implemented) ---
Current chemistry ids = 4 dataset groups (Li-ion=MIX_large, CALB, Zn-ion, Na-ion). Finer options:
  - source-dataset id (~17 values; already exists as datasetName2ids / dataset_id in the loader)
  - cathode type (tables in assets/Further_details_of_data_statistics.md; pkl has a cathode_material field):
    LFP (MATR, HUST, SNL_LFP), LCO (CALCE), NCM variants, NCA, NCM+NCA blend (Tongji3), MnO2 (Zn), Na (unknown)
Note: CALB is Li-ion NCM/graphite (prismatic), so "CALB vs Li-ion" is a dataset split, not a chemistry difference.

--- Mamba / full-timescale unified trainer: decisions so far ---
Goal: same unified (pooled) training as now, but train on longer horizons (all cycles available) with Mamba, on lithium
first (small chemistries: "we try"; S4 ablation and P100 kernel compatibility are LATER concerns).
- Training samples = prefixes 1..i of a cell (final CLS token, classify only at the final timestep). Optional
  every-timestep prediction is a later training-loop option, not a loader concern.
- Sample balancing: chemistry-agnostic PER-CELL CAP - at most K prefixes per cell per epoch (K specified in config),
  resampled each epoch. Preferred over a per-chemistry drop % (simpler; also fixes long-vs-short cells within a chemistry).
  Print each chemistry's share of training samples per epoch.
- Val/test stay exactly as published (first 100 cycles, same cells). No prefix-length-bucket logging; val errors are enough.
- Data facts: many CALB cells only store 100 cycles (median life ~1170); Na-ion max ~250 stored cycles; Zn-ion median 168
  (35 of 140 files have >=300). Long cells: HUST (77 cells, median 1873), ISU_ILCC, MATR.
  Sum of usable cycles ~730k vs ~106k capped at 100 (stats file, all sources).
- MIX_all (1001 cells) = MIX_large (843, 14 source datasets) + ZN-coin 100 + CALB 27 + NA-ion 31, i.e. the four pooled groups
  cover every labeled cell in the published splits. Two sources exist outside them: SDU (86 files, 70 labeled, added 2025.10)
  and Stanford_2 (129 usable / 181 labeled; "complete Stanford" - probably OVERLAPS the 41 Stanford cells in MIX_large,
  so check for test-set leakage before using it as extra training data).
Status: revisit the Mamba dataloader after the pooled sweep is submitted.


=== Full-timescale (Mamba) dataloader: built, end of session 9/19 ===

WHAT EXISTS (committed; already pulled on SCC since precompute ran there)
- data_provider/data_loader_full.py    Dataset_full_timescale + my_collate_fn_full + cache builder
- data_provider/data_factory.py        + data_provider_full(args, chemistries, split_seed)
- precompute_full_cells.py             builds the on-disk cycle cache (separate step)
- check_full_equivalence.py            real-data check vs the baseline loader (NOT RUN YET)
- smoke_test_full.py                   prints shapes / per-chemistry shares (NOT RUN YET)
Only tested on synthetic fake pkls so far (cache contents, prefix rules, K cap, epoch redraw, collate).

DESIGN (as agreed)
- Same pooled train cells + chemistry id + same exclusions (no label, no pkl, eol <= 100).
- Prefixes 1..min(stored cycles, eol-1) per cell (not capped at 100).
- --full_max_cycles N      longest prefix used. empty = no max.
- --full_prefixes_per_cell K   at most K random prefixes per cell per epoch, redrawn each epoch (seeded by seed+epoch),
                            chemistry-agnostic. empty = drop nothing. Cells with < K prefixes contribute all.
- Batch size 1, NO padding. Batch layout = pooled 8-tuple (curves [1,L,3,300], mask [1,L] all ones, labels, life_class,
  scaled_life_class, weights, seen_unseen, chemistry_ids). Assert in collate if batch > 1.
- Call data_set.set_epoch(e) before iterating each epoch (prints per-chemistry samples / cycle-steps / shares).
- Val/test unchanged: existing per-chemistry Dataset_pooled loaders, prefixes 1..100, published cells.
- Cache = <root_path>/full_cycle_cache/<cell>.npy ([n_cycles,3,300] float32, cycles 1..eol-1) + <cell>.meta.json.
  max_cycles applied at load time, so changing it never needs a rebuild. Loader errors with the exact command if cache missing.
- Curves use the baseline's own get_charge_discharge_curves, one cycle at a time (same per-cycle code, O(N) not O(N^2)).
- Unusable cycle (exception / NaN / no rows): cache for that cell STOPS before it, recorded in meta
  (truncated_at_cycle, reason); precompute prints these at the end. Worth a look once it finishes.
- label scaler is fit on the included cells' eol, i.e. the same cells Dataset_pooled(train) fits on. Hand it to val/test loaders.
- Not wired into run_main.py (no Mamba model / training loop yet). --weighted_loss not supported by this loader.

PRECOMPUTE STATUS (interrupted)
- Ran: python precompute_full_cells.py --root_path ./dataset --workers 4
- 664 training cells (union of train lists across split seeds 2021/42/2024; the 2021 pooled train set alone is 612).
- Reached ~400/664 cells in 455 s, then the qrsh session hit its time limit. Order was RWTH, HUST, MATR, Stanford, Tongji,
  ISU-ILCC (in progress). Long HUST cells were slowest (~8-10 s each); one ISU-ILCC cell took 18.7 s.
- Safe to just rerun the same command: finished cells are skipped, .npy is written via tmp+rename and the .meta.json last,
  so a cell killed mid-write is redone. Cells not reached yet: rest of ISU-ILCC + everything after (XJTU, MICH, SNL,
  UL-PUR, CALB, ZN-coin, NA-ion, ... whatever follows in the list).
- Do it in a job or a longer session next time: qsub a CPU-only job (or qrsh with a longer h_rt) so it can't be cut off.

NEXT (in order)
1. Rerun precompute until it prints the final summary (status counts, cycles stored, truncated cells, cache size in GB).
2. python check_full_equivalence.py     (first 100 cycles == baseline read_cell_df incl. a Zn charge-first cell; counts + scaler
                                         match Dataset_pooled for CALB/Zn/Na at max_cycles=100)
3. python smoke_test_full.py --full_prefixes_per_cell 50      (shapes, shares, epoch redraw)
4. Commit the notes + any fixes that come out of the checks.
5. Then: Mamba model + training loop (CLS token at final position, single prediction per prefix; per-step prediction later,
   training-loop concern). P100 compatibility of the Mamba kernels and the S4 ablation are still later concerns.

STILL PARKED
- --chem_loss_alpha (per-chemistry loss weighting), finer conditioning (cathode / source dataset), Stanford_2 leakage check before use.
- The 8 pooled sweep jobs (7651962-7651969) were in the queue last I looked; results not collected yet.

=== notes 9/22 ===

--- early_concat fusion + chem_loss_alpha: implemented ---
early_concat (chemistry embedding concatenated onto the raw flattened input, before the model's first layer,
so every downstream layer sees it -- not just the head like late_mlp) finished end to end for both CPMLP and
CPTransformer: ChemEarlyEmbed (layers/ChemHead.py), model wiring, --chem_fusion CLI choice, test_chem_models.py
coverage (determinism, chemistry changes output, per-row independence, gradient flow, embed_dim=0 capacity
control has IDENTICAL param count to chem_fusion=none). CPMLP's half of this already existed uncommitted from
an earlier session; CPTransformer + CLI + tests were the gap, now closed.

--chem_loss_alpha (the per-chemistry loss-volume-weighting idea parked above on 9/19) also implemented:
Dataset_pooled.volume_weights staticmethod, weight = N_c^-alpha rescaled to mean 1, composes with
--weighted_loss rather than overriding it. Verified with a standalone numpy test against the real shipped
function (extracted source + exec'd, since no torch in this shell): alpha=0 no-op, alpha=1 exactly equalizes
total loss mass per chemistry, alpha=0.5 in between, --pooled_chemistries subset case doesn't spuriously trip
the "every chemistry needs positive count" assert. Not yet run on real data/GPU.

--- Regularization gap found, staged dropout/wd sweep built ---
Checked what regularization pooled training actually had: --dropout was hardcoded to 0 in the qsub scripts
(the Li-ion row's tuned value -- CALB/Zn-ion/Na-ion's own tuned dropout was actually 0.05-0.1) and --wd was
never set (default 0.0). Only real regularization in effect was early stopping (patience 5 for pooled vs 10
default). A model trained on 4x the data with an 84%-Li-ion imbalance is a different regularization regime
than any single per-chemistry baseline, so this was worth sweeping rather than assuming.

Decided against a full factorial (dropout x wd x fusion x alpha ~= 150-300 jobs) in favor of a STAGED search:
  Stage 1 (submit_dropout_wd_sweep.sh, new): DROPOUT {0, 0.05, 0.1} x WD {0, 1e-4, 1e-3}, chem_fusion=none,
    chem_loss_alpha=0. Skips the (0, 0.0) cell -- that's the existing 9/19-9/20 baseline. 32 jobs.
  Stage 2 (submit_pooled_sweep.sh, rewritten): chem_fusion {none, late_mlp16, early_concat16} x chem_loss_alpha
    {0, 0.5, 1.0}, with DROPOUT/WD passed in as fixed env vars set to stage 1's winner
    (e.g. DROPOUT=0.1 WD=0.0001 bash train_eval_scripts/submit_pooled_sweep.sh). 36 jobs (44 w/ WITH_CONTROL=1).
Both DRY_RUN-verified (job counts, naming). Nothing submitted yet, nothing committed yet.

NEXT (in order)
1. git add -A && git commit, push, pull on SCC.
2. python test_chem_models.py on SCC (torch venv active) -- verify fusion changes before spending GPU time.
3. bash train_eval_scripts/pooled_quick_test.sh -- quick sanity pass (all variants incl. early_concat, 2 epochs).
4. Once quick test passes: bash train_eval_scripts/submit_dropout_wd_sweep.sh (stage 1, 32 jobs).
5. After stage 1 results: DROPOUT=<winner> WD=<winner> bash train_eval_scripts/submit_pooled_sweep.sh (stage 2).

STILL PARKED
- late_linear variant (concat -> single linear = per-chemistry offset only).
- Finer conditioning granularity (source-dataset id, cathode type) -- see 9/19 note above, unchanged.
- Full-timescale (Mamba) dataloader work -- untouched this session, see 9/19 section above; precompute status
  as of 9/19 was ~400/664 cells done, needs a rerun in a job with longer h_rt.

--- Stage 1 (dropout x wd sweep) submitted 9/22 ---
qsub'd via submit_dropout_wd_sweep.sh: 32 jobs, SCC job ids 7692641-7692672 (cpmlp_pool_* / cptransformer_pool_*,
chem_fusion=none, chem_loss_alpha=0, sweeping DROPOUT {0,0.05,0.1} x WD {0,1e-4,1e-3}, (0,0.0) baseline skipped).

TODO: once stage 1 results are in, pick the winning (dropout, wd) by macro val MAPE, then submit stage 2 (the
early/late conditioning + chem_loss_alpha sweep, NOT yet submitted -- this is where early_concat gets tested):
  DROPOUT=<winner> WD=<winner> bash train_eval_scripts/submit_pooled_sweep.sh

--- Full-timescale precompute finished, loader verified (9/22) ---
Reran `python precompute_full_cells.py --root_path ./dataset --workers 4` in a live qrsh session (the 9/19 attempt
had died at ~400/664 cells when that session hit its time limit). This time it completed cleanly: 664/664 cells
processed in 125s total elapsed (the ~400 already-cached cells from the interrupted run were skipped near-instantly;
real work was the remaining ~264, starting around CALCE/SNL/MICH/CALB/ZN-coin/NA-ion).

Final status: {'ok': 657, 'no_cycles': 2, 'no_label': 5} -- same flavor of exclusions the baseline already drops.
483,599 total cycles stored across the 657 ok cells, longest single cell 4998 cycles. Cache size 1.74 GB.

Two cells truncated early during the cache build:
  SNL_18650_NCA_25C_0-100_0.5-0.5C_b.pkl: stopped at cycle 136 of 179 (IndexError: index -1 out of bounds, empty
    charge/discharge segment on that cycle)
  SNL_18650_NCA_25C_20-80_0.5-0.5C_c.pkl: stopped at cycle 191 of 791 (same IndexError)
Not fatal -- meta.json records truncated_at_cycle and the loader just uses what's cached -- but worth a look before
these two cells matter for real training, since the range read for them is shorter than eol would suggest.

Ran the two verification scripts against the finished cache:
  python check_full_equivalence.py        -- passed
  python smoke_test_full.py --full_prefixes_per_cell 50   -- passed (shapes, epoch redraw, full pass over one
    epoch: "loaded 300 samples OK (stopped at 300)")

This closes out the full-timescale loader work item from the 9/19 section above (dataloader + cache + equivalence
checks are done and verified end to end).

NEXT (in order)
1. Commit the full-timescale loader code (data_provider/data_loader_full.py, precompute_full_cells.py,
   check_full_equivalence.py, smoke_test_full.py) + these notes -- nothing from this work is committed yet.
2. Start the Mamba model + training loop against Dataset_full_timescale. Plan as of 9/19: CLS-token-style single
   prediction per prefix first (same one-label-per-sequence shape as current models), per-step prediction deferred
   as a training-loop question for later. P100 kernel compatibility for the Mamba CUDA kernels and an S4 ablation
   are later concerns, not blockers for the first pass.
